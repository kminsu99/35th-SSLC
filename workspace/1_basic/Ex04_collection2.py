"""
보안 사고가 의심되는 서버를 격리(Quarantine)했는지 관리·확인하는 작업을 표현한 예제입니다.

[ 전체 작업 ]
    보안 관제 담당자가 격리 대상 서버 목록을 관리하면서, 
    특정 서버가 이미 격리되었는지 확인하고 현재 몇 대의 서버가 격리되어 있는지 점검하는 작업

[ 예를 들어 ]
    prd-db-02에서 악성 행위가 탐지되었다면, 
    이미 네트워크 격리 조치가 되었는지 확인하고 전체 격리 서버 현황을 파악하는 상황입니다.
"""

quarantined_hosts = {"prd-bastion-01", "prd-db-02"}
target_host = "prd-db-02"




"""
보안 취약점의 CVSS 위험도 점수를 분석하여 전체적인 위험 수준을 파악하는 작업입니다.

[ 전체 작업 ]
    여러 취약점의 CVSS 점수를 수집한 후, 가장 위험한 취약점과 가장 낮은 취약점을 확인하고, 
    전체 위험도와 위험도 순위를 분석하는 작업

[ 예를 들어 ]
    여러 취약점의 CVSS 점수를 수집한 후, 가장 위험한 취약점과 가장 낮은 취약점을 확인하고, 
    전체 위험도와 위험도 순위를 분석하는 상황입니다.

[ 참고 ] CVSS 
     CVSS(Calculated Vulnerability Scoring System)는 취약점의 위험도를 수치화하는 표준입니다.
    |        점수 | 위험도 |
    | ---------: | ---  |
    |        0.0 | 없음  |
    |  0.1 ~ 3.9 | 낮음  |
    |  4.0 ~ 6.9 | 중간  |
    |  7.0 ~ 8.9 | 높음  |
    | 9.0 ~ 10.0 | 치명적 |

"""


cvss_scores = [4.2, 9.8, 6.5, 7.1, 2.0]



